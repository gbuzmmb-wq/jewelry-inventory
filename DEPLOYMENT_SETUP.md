# 🚀 Настройка автоматического деплоя на GitHub Pages

## ✅ Что уже сделано

1. ✅ Создан workflow файл `.github/workflows/deploy.yml` для автоматического деплоя
2. ✅ Настроен автоматический деплой при каждом push в ветку `main`

## 📋 Пошаговая инструкция

### Шаг 1: Включить GitHub Pages в настройках репозитория

1. Откройте ваш репозиторий на GitHub: `https://github.com/ВАШ_НИКНЕЙМ/jewelry-inventory`
2. Перейдите в **Settings** (Настройки)
3. В левом меню найдите раздел **Pages**
4. В разделе **Source** (Источник):
   - Выберите **GitHub Actions** вместо **Deploy from a branch**
5. Нажмите **Save** (Сохранить)

### Шаг 2: Проверить remote репозиторий

Если в вашем remote отсутствует имя пользователя, нужно его исправить:

```bash
# Проверить текущий remote
git remote -v

# Если нужно обновить (замените YOUR_USERNAME на ваш GitHub никнейм)
git remote set-url origin https://github.com/YOUR_USERNAME/jewelry-inventory.git
```

### Шаг 3: Закоммитить и отправить изменения

```bash
# Добавить все файлы
git add .

# Создать коммит
git commit -m "feat: Настроен автоматический деплой через GitHub Actions"

# Отправить на GitHub
git push origin main
```

### Шаг 4: Проверить деплой

1. Перейдите на вкладку **Actions** в вашем репозиторий на GitHub
2. Вы увидите запущенный workflow "🚀 Автоматический деплой на GitHub Pages"
3. Дождитесь завершения (обычно 1-2 минуты)
4. После успешного деплоя, ваше приложение будет доступно по адресу:
   ```
   https://YOUR_USERNAME.github.io/jewelry-inventory/
   ```

## 🔄 Как работает автоматический деплой

- **Автоматически**: При каждом push в ветку `main` запускается деплой
- **Вручную**: Можно запустить из вкладки **Actions** → выберите workflow → **Run workflow**

## 🛠️ Устранение проблем

### Проблема: "Pages build failed"

**Решение**: 
1. Проверьте логи в разделе **Actions**
2. Убедитесь, что все файлы проекта включены в репозиторий
3. Проверьте, что в репозиторий есть файл `index.html`

### Проблема: "GitHub Pages не работает"

**Решение**:
1. Убедитесь, что в настройках репозитория выбран **GitHub Actions** как источник
2. Проверьте, что workflow завершился успешно (зелёная галочка в Actions)
3. Подождите 1-2 минуты после деплоя (иногда требуется время для обновления)

### Проблема: "Remote repository не найден"

**Решение**:
1. Проверьте правильность URL в `git remote -v`
2. Если нужно обновить:
   ```bash
   git remote set-url origin https://github.com/YOUR_USERNAME/jewelry-inventory.git
   ```

## 📝 Полезные команды

```bash
# Проверить статус
git status

# Проверить remote
git remote -v

# Обновить remote
git remote set-url origin https://github.com/YOUR_USERNAME/jewelry-inventory.git

# Добавить и закоммитить изменения
git add .
git commit -m "Описание изменений"
git push origin main
```

## 🎉 Готово!

После настройки каждый раз, когда вы делаете `git push`, ваше приложение автоматически обновляется на GitHub Pages!

