# 🔐 Руководство по авторизации

## Система ролей

Приложение поддерживает две роли пользователей с разными правами доступа.

### 👑 Администратор

**Логин**: `admin`
**Пароль**: `admin`

#### Права доступа:

- ✅ Добавление товаров
- ✅ Редактирование товаров
- ✅ Удаление товаров
- ✅ Отметка товаров как проданных
- ✅ **Отмена продаж** (только администратор может вернуть товар в статус "В наличии")
- ✅ Экспорт/импорт данных
- ✅ Загрузка тестовых данных
- ✅ Печать отчетов
- ✅ Просмотр аналитики

### 💼 Продавец

**Логин**: `seller`
**Пароль**: `seller`

#### Права доступа:

- ✅ Просмотр товаров
- ✅ Отметка товаров как проданных
- ❌ Добавление товаров
- ❌ Редактирование товаров
- ❌ Удаление товаров
- ❌ Отмена продаж
- ❌ Экспорт/импорт данных
- ❌ Печать отчетов

## Вход в систему

1. При первом открытии приложения появится модальное окно авторизации
2. Введите логин и пароль
3. Нажмите "Войти"
4. Модальное окно закроется и вы войдете в систему

## Интерфейс в зависимости от роли

### Администратор видит:

- Все кнопки действий (Добавить, Экспорт, Импорт, Печать, Тестовые данные)
- Кнопки редактирования и удаления в таблице товаров
- Кнопку отмены продажи для проданных товаров

### Продавец видит:

- Только кнопку "Продано" для товаров в наличии
- Никаких кнопок управления данными
- Может только просматривать и отмечать товары как проданные

## Сценарии использования

### Сценарий 1: Администратор добавляет товар

1. Войдите как `admin` / `admin`
2. Нажмите "Добавить товар"
3. Заполните форму
4. Сохраните

### Сценарий 2: Продавец отмечает товар как проданный

1. Войдите как `seller` / `seller`
2. В таблице найдите товар со статусом "В наличии"
3. Нажмите зеленую кнопку с галочкой
4. Товар автоматически перейдет в статус "Продано"

### Сценарий 3: Администратор отменяет продажу

1. Войдите как `admin` / `admin`
2. В таблице найдите товар со статусом "Продано"
3. Нажмите оранжевую кнопку с иконкой отмены
4. Подтвердите действие
5. Товар вернется в статус "В наличии"

### Сценарий 4: Продавец пытается изменить товар

1. Войдите как `seller` / `seller`
2. Попытайтесь нажать кнопку редактирования
3. Кнопка будет скрыта (невидима)
4. Никакие изменения невозможны

## Выход из системы

1. Нажмите на имя пользователя в правом верхнем углу
2. Выберите "Выйти"
3. Подтвердите выход
4. Модальное окно авторизации появится снова

## Безопасность

- ✅ Авторизация через sessionStorage (сессия)
- ✅ Автоматический выход при закрытии браузера
- ✅ Разграничение прав на уровне UI
- ✅ Повторная авторизация при каждом открытии (опционально)

## Изменение учетных данных

Учетные данные хранятся в коде приложения. Для изменения:

Откройте `assets/js/app.js` и найдите:

```javascript
this.credentials = {
  admin: { password: 'admin', role: 'admin', name: 'Администратор' },
  seller: { password: 'seller', role: 'seller', name: 'Продавец' },
};
```

Измените пароли на нужные.

**Важно**: В production версии храните пароли в зашифрованном виде на сервере!

## Добавление новых пользователей

Для добавления новых пользователей:

```javascript
this.credentials = {
  admin: { password: 'admin', role: 'admin', name: 'Администратор' },
  seller: { password: 'seller', role: 'seller', name: 'Продавец' },
  manager: { password: 'manager123', role: 'seller', name: 'Менеджер' },
};
```

Новый пользователь `manager` будет иметь права продавца.

## Рекомендации для production

1. **Используйте backend сервер** для хранения учетных данных
2. **Хешируйте пароли** (bcrypt, argon2)
3. **Используйте JWT** для аутентификации
4. **Логируйте действия** пользователей
5. **Используйте HTTPS** для безопасности
6. **Реализуйте смену пароля** для пользователей
7. **Добавьте восстановление пароля**

---

**Примечание**: Текущая версия предназначена для демонстрации и тестирования. Для production использования необходима интеграция с backend-сервером.
