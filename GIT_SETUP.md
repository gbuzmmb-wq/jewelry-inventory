# 🚀 Инструкция: Загрузка на GitHub (простой способ)

## Первый раз - загрузка проекта

### Вариант 1: Через GitHub Desktop (САМЫЙ ПРОСТОЙ)

1. **Скачайте GitHub Desktop**: https://desktop.github.com/
2. **Установите** и откройте
3. **File → Add Local Repository**
4. Выберите папку `Dzhamily`
5. **Напишите коммит**: "Initial commit - Jewelry Inventory"
6. **Нажмите "Publish repository"**
7. Выберите имя: `jewelry-inventory` (или любое другое)
8. **Готово!**

**Ссылка будет**: `https://github.com/ВАШ_NICKNAME/jewelry-inventory`

### Вариант 2: Через веб-интерфейс GitHub

1. Откройте https://github.com
2. Нажмите "+" → **"New repository"**
3. Имя: `jewelry-inventory`
4. **НЕ** ставьте галочку "Initialize with README"
5. Нажмите **"Create repository"**
6. На странице репозитория нажмите **"uploading an existing file"**
7. Перетащите **ВСЕ** файлы из папки `Dzhamily`:
   - index.html
   - START_HERE.md
   - README.md
   - все .md файлы
   - sample-data.json
   - favicon.svg
   - папку assets/
8. Нажмите **"Commit changes"**
9. **Готово!**

### Вариант 3: Через командную строку (для продвинутых)

Откройте Git Bash в папке проекта и выполните:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_NICKNAME/jewelry-inventory.git
git push -u origin main
```

## Последующие обновления (простой способ)

### Если используете GitHub Desktop:

1. **GitHub Desktop** откроется автоматически
2. **В левой панели** будут показаны измененные файлы
3. **Напишите описание изменений**: например "Added auth system"
4. **Нажмите "Commit to main"**
5. **Нажмите "Push origin"**
6. **Готово!** Изменения загружены

### Если используете веб-интерфейс:

**Просто загрузите только измененные файлы** через веб-интерфейс:
1. Нажмите **"Add file"** → **"Upload files"**
2. Перетащите только измененные файлы
3. Нажмите **"Commit changes"**

### Если используете командную строку:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## Включить GitHub Pages

1. Откройте репозиторий на GitHub
2. **Settings** (вкладка сверху)
3. **Pages** (в левом меню)
4. **Source**: Deploy from a branch
5. **Branch**: main, folder: / (root)
6. **Save**
7. Подождите 2-3 минуты
8. Ваше приложение: `https://ВАШ_NICKNAME.github.io/jewelry-inventory/`

## Что НЕ нужно делать каждый раз:

❌ **НЕ** удаляйте старые файлы  
❌ **НЕ** создавайте новый репозиторий  
❌ **НЕ** загружайте все файлы заново  

## Что нужно делать:

✅ Загрузите **только** измененные файлы  
✅ Напишите описание изменений  
✅ Это все!  

## Бонус: Автоматизация (опционально)

Создайте файл `update.bat` для Windows:

```batch
@echo off
git add .
git commit -m "Update"
git push
echo Done!
pause
```

Или `update.sh` для Linux/Mac:

```bash
#!/bin/bash
git add .
git commit -m "Update"
git push
echo "Done!"
```

Затем просто запускайте этот файл каждый раз!

---

**Самый простой вариант**: Используйте **GitHub Desktop** - там все визуально и не нужно помнить команды!

