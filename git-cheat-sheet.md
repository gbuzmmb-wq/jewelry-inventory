# 📝 Git шпаргалка - Самые нужные команды

## 🆘 Если ничего не знаешь о Git:

### Вариант 1: GitHub Desktop
1. Скачай: https://desktop.github.com/
2. Установи
3. Открой GitHub Desktop
4. **File** → **Add Local Repository**
5. Выбери папку `Dzhamily`
6. **Publish repository**
7. **Готово!**

### Вариант 2: Веб-интерфейс
1. Открой https://github.com
2. Создай новый репозиторий
3. Перетащи файлы
4. **Готово!**

---

## 🎯 Если хочешь использовать командную строку:

### 📤 Первый раз - загрузка проекта

```bash
# Начальная настройка (один раз)
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_NICKNAME/jewelry-inventory.git
git push -u origin main
```

### 🔄 Каждый раз - обновление проекта

```bash
# Три команды - всё!
git add .
git commit -m "Описание изменений"
git push
```

**Готово!** Изменения загружены на GitHub.

---

## 📋 Полезные команды

### Посмотреть что изменилось
```bash
git status
```

### Отменить последний коммит (если ошибся)
```bash
git reset --soft HEAD~1
```

### Посмотреть историю
```bash
git log --oneline
```

### Клонировать чужой проект
```bash
git clone https://github.com/ПОЛЬЗОВАТЕЛЬ/репозиторий.git
```

---

## 🎬 Сценарии

### Сценарий 1: Я добавил новые файлы
```bash
git add .
git commit -m "Added new files"
git push
```

### Сценарий 2: Я изменил существующие файлы
```bash
git add .
git commit -m "Updated files"
git push
```

### Сценарий 3: Ошибка - хочу вернуть всё назад
```bash
git reset --hard HEAD~1
git push -f
```

⚠️ **Осторожно!** Это удалит последний коммит.

---

## 🤖 Автоматизация

### Windows: создай файл `update.bat`

```batch
@echo off
git add .
git commit -m "Update %date% %time%"
git push
pause
```

**Использование**: Дважды кликни на файл - всё загрузится!

### Linux/Mac: создай файл `update.sh`

```bash
#!/bin/bash
git add .
git commit -m "Update"
git push
```

**Использование**: 
```bash
chmod +x update.sh
./update.sh
```

---

## ❓ Частые вопросы

### Q: "Git не установлен"
**A**: Скачай: https://git-scm.com/downloads

### Q: "Permission denied"
**A**: Используй GitHub Desktop или веб-интерфейс

### Q: "Remote origin already exists"
**A**: 
```bash
git remote remove origin
git remote add origin https://github.com/ТВОЙ_NICKNAME/repo.git
```

### Q: "Push failed"
**A**: Попробуй:
```bash
git pull
git push
```

---

## 🎓 Рекомендации

1. **Начинающим**: GitHub Desktop
2. **Любителям GUI**: GitHub Desktop
3. **Продвинутым**: Командная строка
4. **Ленивым**: Веб-интерфейс

---

**Запомни**: `add` → `commit` → `push` = готово! ✅

